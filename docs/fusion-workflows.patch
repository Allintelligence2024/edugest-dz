diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 2150e29..3e351a6 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -45,7 +45,7 @@ jobs:
 
     defaults:
       run:
-        working-directory: edugestdz/backend
+        working-directory: backend
 
     steps:
       - uses: actions/checkout@v4
@@ -66,7 +66,7 @@ jobs:
         uses: actions/cache@v4
         with:
           path: ${{ steps.composer-cache.outputs.dir }}
-          key: ${{ runner.os }}-composer-${{ hashFiles('edugestdz/backend/composer.lock') }}
+          key: ${{ runner.os }}-composer-${{ hashFiles('backend/composer.lock') }}
           restore-keys: ${{ runner.os }}-composer-
 
       - name: Install dependencies
@@ -151,7 +151,7 @@ jobs:
             exit 1
           fi
           echo "OK — aucun jeton persisté côté navigateur"
-        working-directory: edugestdz
+        working-directory: .
 
       - name: "Sécurité — aucun secret en dur"
         run: |
@@ -232,7 +232,7 @@ jobs:
     runs-on: ubuntu-latest
     defaults:
       run:
-        working-directory: edugestdz/frontend
+        working-directory: frontend
 
     steps:
       - uses: actions/checkout@v4
@@ -242,7 +242,7 @@ jobs:
         with:
           node-version: '22'
           cache: 'npm'
-          cache-dependency-path: edugestdz/frontend/package-lock.json
+          cache-dependency-path: frontend/package-lock.json
 
       - name: Install dependencies
         run: npm ci
@@ -275,16 +275,16 @@ jobs:
 
       - name: "Audit des dépendances PHP (composer audit) — non bloquant : advisories laravel/framework 11.31 à résoudre par composer update (voir dette Lot A)"
         continue-on-error: true
-        working-directory: edugestdz/backend
+        working-directory: backend
         run: composer audit --no-interaction
 
       - name: Audit des dépendances npm du frontend (bloquant)
-        working-directory: edugestdz/frontend
+        working-directory: frontend
         run: npm audit --audit-level=high
 
       - name: Audit des dépendances npm du mobile (non bloquant)
         if: always()
-        working-directory: edugestdz/mobile
+        working-directory: mobile
         continue-on-error: true
         run: npm audit --audit-level=high
 
@@ -297,7 +297,7 @@ jobs:
         continue-on-error: true
 
       - name: Analyse statique backend (PHPStan niveau 6, bloquant avec baseline)
-        working-directory: edugestdz/backend
+        working-directory: backend
         run: ../../scripts/analyse-statique.sh --no-progress
 
       - name: "Garde-fou — contrôleurs ≤350 lignes (baseline Lot C)"
diff --git a/.github/workflows/deploy.yml b/.github/workflows/deploy.yml
index a65266c..8487cba 100644
--- a/.github/workflows/deploy.yml
+++ b/.github/workflows/deploy.yml
@@ -49,7 +49,6 @@ jobs:
             echo "📥 Pull des dernières modifications..."
             git pull origin main
 
-            cd edugestdz
 
             echo "🚀 Déploiement..."
             chmod +x deploy.sh
diff --git a/.github/workflows/pre-deploy-check.yml b/.github/workflows/pre-deploy-check.yml
index cda9029..2033bdb 100644
--- a/.github/workflows/pre-deploy-check.yml
+++ b/.github/workflows/pre-deploy-check.yml
@@ -43,7 +43,7 @@ jobs:
 
     defaults:
       run:
-        working-directory: edugestdz/backend
+        working-directory: backend
 
     steps:
       - uses: actions/checkout@v4
@@ -63,7 +63,7 @@ jobs:
         uses: actions/cache@v4
         with:
           path: ${{ steps.composer-cache.outputs.dir }}
-          key: ${{ runner.os }}-composer-${{ hashFiles('edugestdz/backend/composer.lock') }}
+          key: ${{ runner.os }}-composer-${{ hashFiles('backend/composer.lock') }}
           restore-keys: ${{ runner.os }}-composer-
 
       - name: Install dependencies
