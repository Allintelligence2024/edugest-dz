# Baseline contrôleurs backend — ligne de départ avant refactor Lot C.
# Format : chemin relatif (depuis racine repo) | taille maximale observée.
#
# Règle du garde-fou (scripts/verifier-controleurs.sh) :
#   - un contrôleur absent de cette baseline doit rester <= SEUIL (350) ;
#   - un contrôleur présent doit rester <= sa taille maximale ici (jamais
#     d'aggravation : les 9 gros contrôleurs seront refactorisés en dessous
#     du seuil, ce qui fera diminuer puis disparaître ces lignes).
backend/app/Http/Controllers/Api/V1/AuthController.php|440
